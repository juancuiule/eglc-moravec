# Root-level compose vars (docker-compose.yml's ${...} substitution reads
# this file automatically — separate from apps/backend/.env and
# apps/frontend/.env, which the containers read directly).

# From the tunnel's connector setup in the Cloudflare Zero Trust dashboard
# (Networks > Tunnels > <tunnel> > Overview > install & run a connector).
CLOUDFLARE_TUNNEL_TOKEN=
